version: 2

models:
  - name: stg_customers
    description: customers from HAW
    columns:
      - name: id
        description: The unique key for each customer
        data_tests:
          - not_null
          - unique